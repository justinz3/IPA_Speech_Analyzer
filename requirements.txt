# Hugging Face Spaces install path. Mirrors [project.dependencies] +
# [project.optional-dependencies.web] in pyproject.toml; keep in sync when
# upgrading deps. Local dev uses `uv sync --extra web` instead.
torch>=2.3,<3.0
torchaudio>=2.3,<3.0
transformers>=4.44,<5.0
soundfile>=0.12,<1.0
numpy>=1.26,<3.0
typer>=0.12,<1.0
protobuf>=4.25,<7.0
phonemizer>=3.2,<4.0
jiwer>=3.0,<4.0
pyyaml>=6.0,<7.0
pypinyin>=0.50,<1.0
pyopenjtalk>=0.3,<1.0
librosa>=0.10,<2.0
gradio>=4.40,<6.0
